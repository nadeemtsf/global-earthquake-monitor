"""
Backend test suite for the Global Earthquake Monitor FastAPI application.

All tests use FastAPI's TestClient (httpx-backed) so they run without
a live server. To execute:

    cd backend
    pytest -q
"""
